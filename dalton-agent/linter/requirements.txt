Flask==3.1.3
gunicorn==23.0.0
# GPL-3.0. Run as an arm's-length subprocess/sidecar reached over HTTP by
# the (Apache-2.0) controller - not linked into or distributed as part of
# the Dalton codebase. See README.rst for the licensing note.
suricata-language-server==2.1.2
